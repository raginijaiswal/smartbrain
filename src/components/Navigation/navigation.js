import React from 'react';

const Navigation =() =>{
    return(
        <nav style ={{display:'flex', justifyContent: 'flex-end'  }}>
            <p className='f3 link dim black pa3 pointer ' style={{zIndex:'1'}}>Sign Out</p>
        </nav>
    )
}

export default Navigation;